{
  "name": "opportunityos",
  "version": "0.1.0-simulation",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test packages/core/test/*.test.mjs packages/postgres/test/*.test.mjs",
    "typecheck": "npm run build:core && npm run typecheck:local && npm run typecheck --workspace @opportunityos/worker && npm run typecheck --workspace @opportunityos/control-plane",
    "build:core": "tsc -p packages/core/tsconfig.json",
    "build": "npm run build:core && npm run build --workspace @opportunityos/postgres && npm run build --workspace @opportunityos/worker && npm run build --workspace @opportunityos/control-plane",
    "dev": "npm run dev --workspace @opportunityos/control-plane",
    "smoke": "node --experimental-strip-types scripts/smoke.ts",
    "typecheck:local": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/postgres/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
